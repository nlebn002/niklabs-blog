# Runbook

Common commands:

```powershell
docker compose up
dotnet build
dotnet test
cd frontend
npm run dev
npm run build
```

Use repo-specific scripts when present. For EF migrations, follow `docs/migrations.md`.

Expected local services are defined in `docker-compose.yml`.

