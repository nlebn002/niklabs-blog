# Implement Spec

Read the active spec in `.ai/specs/` and all relevant `.ai/context/` files.

Implement the spec end to end. Keep changes scoped, follow existing patterns, update OpenAPI/Orval when contracts change, and run focused verification before handoff.

